{
    "configurations": [
        {
            "name": "Linux",
            "includePath": [
                "/usr/include/linux",
                "${workspaceFolder}/**",
                "${default}"
            ],
            "defines": [ 
                "${default}",
            ],
            "compilerPath": "${default}",
            "compilerArgs": [ "-Wno-unused-parameters", "-Wno-unused-variable", "-Wno-missing-field-initializers" ],
            "cStandard": "${default}",
            "cppStandard": "${default}",
            "intelliSenseMode": "linux-gcc-x64",
            "browse": {
                "path" : [
                    "${workspaceFolder}",
                    "${default}"
                ]
            }
        }
    ],
    "version": 4
}
